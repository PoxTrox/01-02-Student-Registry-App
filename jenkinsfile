pipeline {
    agent any 
    stages {
        stage('Install Dependencies') { 
            steps {
                bat 'npm install' &
                bat 'npm run test'
            }
        } 
    }
}