pipeline {
    agent any 
    stages {
        stage('Install Dependencies') { 
            steps {
                bat 'npm install'
            }
            steps('Execute tests') {
                bat 'npm run test'
            }
        } 
    }
}